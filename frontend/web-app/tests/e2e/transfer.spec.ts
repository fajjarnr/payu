/**
 * PayU E2E shim — assignment expects tests/e2e/transfer.spec.ts
 * Canonical implementation lives at e2e/transfer.spec.ts (testDir: ./e2e).
 * This file mirrors it for path-checks and is ignored by playwright (testIgnore: tests/**).
 * It is kept in sync via copy — see ../e2e/transfer.spec.ts.
 */
export {};
