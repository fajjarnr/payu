payu:
  security:
    # enabled: true (default — matchIfMissing=true)
    encryption-enabled: false
    # masking-enabled: true (default — matchIfMissing=true)
    # audit-enabled: true (default — matchIfMissing=true)
