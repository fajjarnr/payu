<?xml version="1.0" encoding="UTF-8"?>
<!--
    PayU Standard Logging Configuration
    Include this in your service's logback-spring.xml:
    <include resource="logback-payu-base.xml"/>

    Features:
    - JSON logging via LogstashEncoder for production
    - MDC support for correlation_id, trace_id, span_id
    - Service metadata (service name, version, environment)
    - LokiStack compatible format
-->
<configuration>
    <!-- Import Spring Boot defaults -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Service metadata from environment (SPRING_APPLICATION_NAME set in Containerfile) -->
    <property scope="context" name="SERVICE_NAME" value="${SPRING_APPLICATION_NAME:-unknown-service}"/>
    <property scope="context" name="SERVICE_VERSION" value="${SERVICE_VERSION:-1.0.0}"/>
    <property scope="context" name="ENVIRONMENT" value="${SPRING_PROFILES_ACTIVE:-dev}"/>
    <property scope="context" name="LOG_LEVEL" value="INFO"/>

    <!-- Keep appenders inside the profiles that reference them. Logback otherwise
         initializes unused appenders and emits startup warnings. -->
    <springProfile name="!staging &amp; !prod &amp; !container">
        <appender name="TEXT_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
                <layout class="id.payu.security.masking.LogbackMaskingFilter">
                    <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{correlation_id:-}] [%X{trace_id:-}] %logger{36} - %msg%n</pattern>
                </layout>
            </encoder>
        </appender>

        <root level="${LOG_LEVEL}">
            <appender-ref ref="TEXT_CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="staging | prod | container">
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeContext>true</includeContext>
                <includeMdc>true</includeMdc>
                <includeStructuredArguments>true</includeStructuredArguments>
                <customFields>{"log_format":"json"}</customFields>
                <fieldNames>
                    <timestamp>@timestamp</timestamp>
                    <message>message</message>
                    <logger>logger</logger>
                    <thread>thread</thread>
                    <level>level</level>
                </fieldNames>
                <jsonGeneratorDecorator class="net.logstash.logback.mask.MaskingJsonGeneratorDecorator">
                    <defaultMask>****</defaultMask>
                    <paths>password,pin,nik,token,accessToken,refreshToken,apiKey,cardNumber,cvv</paths>
                    <valueMask>
                        <value>(\w{1})[\w.]+@([\w.]+)</value>
                        <mask>$1***@$2</mask>
                    </valueMask>
                    <valueMask>
                        <value>(\d{3})\d{10}(\d{3})</value>
                        <mask>$1**********$2</mask>
                    </valueMask>
                    <valueMask>
                        <value>(\d{4})\d{8}(\d{4})</value>
                        <mask>$1********$2</mask>
                    </valueMask>
                    <valueMask>
                        <value>(?i)("(?:password|pin|token|api[_-]?key)"\s*:\s*")[^"]+("?)</value>
                        <mask>$1****$2</mask>
                    </valueMask>
                </jsonGeneratorDecorator>
            </encoder>
        </appender>

        <appender name="ASYNC_JSON" class="ch.qos.logback.classic.AsyncAppender">
            <appender-ref ref="JSON_CONSOLE"/>
            <queueSize>512</queueSize>
            <discardingThreshold>0</discardingThreshold>
            <neverBlock>true</neverBlock>
        </appender>

        <root level="${LOG_LEVEL}">
            <appender-ref ref="ASYNC_JSON"/>
        </root>
    </springProfile>

    <!-- Reduce noise from third-party libraries -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.apache.kafka" level="WARN"/>
    <logger name="io.micrometer" level="WARN"/>
</configuration>
