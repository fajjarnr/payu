# Test Configuration for Billing Service
# This configuration is in addition to the %test profile in the main application.yml

# Reactive Messaging: Use In-Memory connector to avoid Kafka dependency/errors in tests
mp.messaging.outgoing.payment-events.connector=smallrye-in-memory

# Ensure hibernate-orm packages are properly configured for tests
quarkus.hibernate-orm.packages=id.payu.billing.domain
