{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://payu.id/schemas/auth-refresh.json",
  "title": "Token Refresh Request",
  "description": "Schema for token refresh requests",
  "type": "object",
  "properties": {
    "refresh_token": {
      "type": "string",
      "minLength": 10,
      "description": "Refresh token (snake_case per OAuth2 spec and auth-service DTO)"
    }
  },
  "required": ["refresh_token"],
  "additionalProperties": false
}
