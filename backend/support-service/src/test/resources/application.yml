quarkus:
  datasource:
    db-kind: h2
    jdbc:
      url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1

  hibernate-orm:
    database:
      generation: drop-and-create

  flyway:
    enabled: false

  log:
    level: WARN
    category:
      "io.quarkus": WARN
      "io.undertow": WARN
