package id.payu.wallet.adapter.persistence.entity;

public enum DiscrepancyType {
        AMOUNT_MISMATCH,
        MISSING_TRANSACTION,
        DUPLICATE_TRANSACTION,
        CURRENCY_MISMATCH,
        TIMING_DIFFERENCE,
        OTHER
    }
