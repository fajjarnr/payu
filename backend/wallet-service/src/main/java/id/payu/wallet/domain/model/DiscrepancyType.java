package id.payu.wallet.domain.model;

public enum DiscrepancyType {
        AMOUNT_MISMATCH,
        MISSING_TRANSACTION,
        DUPLICATE_TRANSACTION,
        CURRENCY_MISMATCH,
        TIMING_DIFFERENCE,
        OTHER
    }
