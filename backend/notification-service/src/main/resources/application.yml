quarkus:
  application:
    name: notification-service
    version: 1.0.0
    environment: dev
  http:
    port: 8080
    auth:
      permission:
        public-health:
          paths: "/public/health,/q/health/*"
          policy: permit
  oidc:
    auth-server-url: ${KEYCLOAK_URL:http://localhost:9080}/realms/${KEYCLOAK_REALM}
    client-id: ${OIDC_CLIENT_ID:notification-service}
    connection-delay: 30S
    tls:
      verification: none
    token:
      issuer: any
  security:
    jaxrs:
      deny-unauthenticated-by-default: true
  otel:
    sdk:
      disabled: ${OTEL_SDK_DISABLED:true}
    service:
      name: notification-service
    exporter:
      otlp:
        endpoint: ${OTEL_ENDPOINT:http://localhost:4317}
    resource:
      attributes:
        - service.name=notification-service
        - service.version=1.0.0
  banner:
    path: banner.txt
  artemis:
    url: ${ARTEMIS_URL:tcp://localhost:61616}
    username: ${ARTEMIS_USERNAME:admin}
    password: ${ARTEMIS_PASSWORD:admin}
  datasource:
    db-kind: postgresql
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD:}
    jdbc:
      url: ${DB_URL:jdbc:postgresql://localhost:5432/notificationdb}
  hibernate-orm:
    packages: id.payu.notification
    database:
      generation: update
    log:
      sql: false
  mailer:
    from: noreply@payu.fajjjar.my.id
    host: smtp.example.com
    port: 587
    start-tls: REQUIRED
    username: ${SMTP_USERNAME:}
    password: ${SMTP_PASSWORD:}
    mock: true
  smallrye-openapi:
    info-title: PayU Notification Service API
    info-version: 1.0.0
    info-description: Notification API for Push, SMS, Email
# SMS provider configuration
payu:
  sms:
    provider: ${SMS_PROVIDER:LOG}
    # twilio:
    #   account-sid: ${TWILIO_SID:}
    #   auth-token: ${TWILIO_TOKEN:}
    #   from-number: ${TWILIO_FROM:}
    # vonage:
    #   api-key: ${VONAGE_KEY:}
    #   api-secret: ${VONAGE_SECRET:}
    # zenziva:
    #   userkey: ${ZENZIVA_USERKEY:}
    #   passkey: ${ZENZIVA_PASSKEY:}
  log:
    level: INFO
    category:
      "id.payu.notification":
        level: DEBUG
    console:
      json:
        enabled: true
        log-format: ecs
        exception-output-type: formatted
        additional-field:
          "service.environment":
            value: dev
            type: STRING
"%dev":
  quarkus:
    hibernate-orm:
      database:
        generation: drop-and-create
    mailer:
      mock: true
mp:
  messaging:
    incoming:
      wallet-events:
        connector: smallrye-kafka
        topic: payu.wallet.balance-changed.v1
        value.deserializer: org.apache.kafka.common.serialization.StringDeserializer
        auto.offset.reset: earliest
        failure-strategy: dead-letter-queue
        dead-letter-queue.topic: payu.wallet.balance-changed.v1.dlq
      transaction-events:
        connector: smallrye-kafka
        topic: payu.transaction.completed.v1
        value.deserializer: org.apache.kafka.common.serialization.StringDeserializer
        auto.offset.reset: earliest
        failure-strategy: dead-letter-queue
        dead-letter-queue.topic: payu.transaction.completed.v1.dlq
      payment-events:
        connector: smallrye-kafka
        topic: payu.transaction.payment-expired.v1
        value.deserializer: org.apache.kafka.common.serialization.StringDeserializer
        auto.offset.reset: earliest
        failure-strategy: dead-letter-queue
        dead-letter-queue.topic: payu.transaction.payment-expired.v1.dlq
      split-bill-events:
        connector: smallrye-kafka
        topic: payu.wallet.split-bill-event.v1
        value.deserializer: org.apache.kafka.common.serialization.StringDeserializer
        auto.offset.reset: earliest
        failure-strategy: dead-letter-queue
        dead-letter-queue.topic: payu.wallet.split-bill-event.v1.dlq
      kyc-verified-events:
        connector: smallrye-kafka
        topic: payu.kyc.verified
        value.deserializer: org.apache.kafka.common.serialization.StringDeserializer
        auto.offset.reset: earliest
        failure-strategy: dead-letter-queue
        dead-letter-queue.topic: payu.kyc.verified.dlq
      kyc-failed-events:
        connector: smallrye-kafka
        topic: payu.kyc.failed
        value.deserializer: org.apache.kafka.common.serialization.StringDeserializer
        auto.offset.reset: earliest
        failure-strategy: dead-letter-queue
        dead-letter-queue.topic: payu.kyc.failed.dlq
