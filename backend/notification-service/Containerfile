# =============================================================================
# PayU notification-service - Multi-stage Quarkus Container Build
# =============================================================================
# Stage 1: Build
# =============================================================================
FROM registry.access.redhat.com/ubi9/openjdk-25:1.24 AS builder

WORKDIR /build
COPY pom.xml .
# Strip <modules> so Maven only discovers what is actually copied
RUN sed '/<modules>/,/<\/modules>/d' pom.xml > pom.xml.tmp && mv pom.xml.tmp pom.xml
COPY notification-service/pom.xml notification-service/
COPY shared/ shared/

RUN mvn -f pom.xml dependency:go-offline -pl notification-service -am -B

COPY notification-service/src notification-service/src
RUN mvn -f pom.xml clean package -DskipTests -pl notification-service -am -B

# =============================================================================
# Stage 2: Runtime
# =============================================================================
FROM registry.access.redhat.com/ubi9/openjdk-25-runtime:1.24

ARG APP_VERSION=1.8.1

LABEL maintainer="backend-team@payu.fajjjar.my.id"
LABEL org.opencontainers.image.title="payu-notification-service"
LABEL org.opencontainers.image.description="PayU notification-service"
LABEL org.opencontainers.image.version="${APP_VERSION}"
LABEL org.opencontainers.image.vendor="PayU Indonesia"
LABEL org.opencontainers.image.source="https://github.com/payu/backend/notification-service"
LABEL id.payu.service.tier="core"

USER root
RUN microdnf update -y && microdnf clean all

USER 185
WORKDIR /deployments

COPY --from=builder --chown=185 /build/notification-service/target/quarkus-app/lib/ /deployments/lib/
COPY --from=builder --chown=185 /build/notification-service/target/quarkus-app/quarkus-run.jar /deployments/quarkus-run.jar
COPY --from=builder --chown=185 /build/notification-service/target/quarkus-app/app/ /deployments/app/
COPY --from=builder --chown=185 /build/notification-service/target/quarkus-app/quarkus/ /deployments/quarkus/

EXPOSE 8080

ENV SPRING_APPLICATION_NAME=notification-service
ENV JAVA_OPTS="-XX:+UseContainerSupport \
    -XX:MaxRAMPercentage=75.0 \
    -XX:+UseG1GC \
    -XX:MaxGCPauseMillis=200 \
    -XX:+HeapDumpOnOutOfMemoryError \
    -XX:HeapDumpPath=/deployments/heapdump.hprof \
    -Dquarkus.http.host=0.0.0.0 \
    -Djava.util.logging.manager=org.jboss.logmanager.LogManager"

HEALTHCHECK --interval=30s --timeout=3s --start-period=60s --retries=3 \
    CMD curl -sf http://localhost:8080/q/health/live || exit 1

ENTRYPOINT ["sh", "-c", "java $JAVA_OPTS -jar /deployments/quarkus-run.jar"]
