quarkus:
  oidc:
    auth-server-url: http://localhost:8180/realms/payu
  devservices:
    enabled: false
  otel:
    sdk:
      disabled: true
    exporter:
      otlp:
        endpoint: http://localhost:4317
