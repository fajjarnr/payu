# PayU MachineAutoscaler — current cluster (cluster-9xtfg, us-east-1)
# Worker pool: m6a.4xlarge (16 vCPU / 64 GiB) in AZ us-east-1f.
# Min 5 = current baseline (46+ pods in payu-dev + promotion environments).
# Max 10 = 160 vCPU / 640 GiB ceiling for SIT/UAT/preprod/prod + k6 load runs.
apiVersion: autoscaling.openshift.io/v1beta1
kind: MachineAutoscaler
metadata:
  name: payu-worker-us-east-1f
  namespace: openshift-machine-api
  annotations:
    payu.io/updated-at: "2026-08-01"
    payu.io/reason: "worker pool growth for promotion environments (DEPLOY-011) and k6 performance runs"
spec:
  minReplicas: 5
  maxReplicas: 10
  scaleTargetRef:
    apiVersion: machine.openshift.io/v1beta1
    kind: MachineSet
    name: cluster-9xtfg-jk2m5-worker-us-east-1f
