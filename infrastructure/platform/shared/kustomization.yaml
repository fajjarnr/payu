apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# PayU Platform - Shared ConfigMaps
# Common configuration used across multiple microservices
# Apply with: oc apply -k infrastructure/platform/shared/

resources:
  - common-configmaps.yaml
