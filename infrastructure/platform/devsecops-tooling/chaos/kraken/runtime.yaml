apiVersion: v1
kind: ServiceAccount
metadata:
  name: cerberus
  namespace: payu-preprod
  labels:
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/component: chaos-engineering
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kraken-chaos
  namespace: payu-preprod
  labels:
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/component: chaos-engineering
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: cerberus-cluster-reader-payu-preprod
  labels:
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/component: chaos-engineering
subjects:
  - kind: ServiceAccount
    name: cerberus
    namespace: payu-preprod
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-reader
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kraken-chaos-admin-payu-preprod
  labels:
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/component: chaos-engineering
subjects:
  - kind: ServiceAccount
    name: kraken-chaos
    namespace: payu-preprod
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
---
apiVersion: v1
kind: Service
metadata:
  name: cerberus
  namespace: payu-preprod
  labels:
    app: cerberus
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/component: chaos-engineering
spec:
  selector:
    app: cerberus
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cerberus
  namespace: payu-preprod
  labels:
    app: cerberus
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/component: chaos-engineering
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cerberus
  template:
    metadata:
      labels:
        app: cerberus
        app.kubernetes.io/part-of: payu
        app.kubernetes.io/component: chaos-engineering
    spec:
      serviceAccountName: cerberus
      containers:
        - name: cerberus
          image: quay.io/redhat-chaos/cerberus:latest
          ports:
            - containerPort: 8080
              name: http
          volumeMounts:
            - name: config
              mountPath: /root/cerberus/config/config.yaml
              subPath: config.yaml
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 15
      volumes:
        - name: config
          configMap:
            name: cerberus-config
---
apiVersion: batch/v1
kind: Job
metadata:
  name: kraken-run
  namespace: payu-preprod
  labels:
    app: kraken
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/component: chaos-engineering
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 1800
  template:
    metadata:
      labels:
        app: kraken
        app.kubernetes.io/part-of: payu
        app.kubernetes.io/component: chaos-engineering
    spec:
      restartPolicy: Never
      serviceAccountName: kraken-chaos
      containers:
        - name: kraken
          image: quay.io/krkn-chaos/krkn:latest
          workingDir: /root/kraken
          command:
            - python
            - run_kraken.py
            - --config
            - /root/kraken/config/config.yaml
          volumeMounts:
            - name: config
              mountPath: /root/kraken/config/config.yaml
              subPath: config.yaml
      volumes:
        - name: config
          configMap:
            name: kraken-config