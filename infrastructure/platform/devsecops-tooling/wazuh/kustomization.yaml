apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: payu-security

helmCharts:
  - name: wazuh
    repo: https://wazuh.github.io/wazuh-kubernetes/
    version: 4.14.4 # Updated per user request
    releaseName: wazuh
    namespace: payu-security
    valuesFile: values.yaml
