apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# PayU Developer Portal (Backstage / RHDH)
# Apply with: oc apply -k infrastructure/platform/devportal/

resources:
  - backstage/app-config.yaml
  - backstage/deployment.yaml
  - backstage/rbac.yaml
  - backstage/rhdh.yaml
  - backstage/secrets.yaml
  - backstage/service.yaml
