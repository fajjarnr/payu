apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# PayU Multi-Region
# Apply with: oc apply -k infrastructure/platform/multi-region/

resources:
  - primary/deployment.yaml
  - secondary/deployment.yaml
  - replication/postgres-replication.yaml
  - replication/kafka-mirroring.yaml
  - failover/failover-job.yaml
  - monitoring/replication-lag-service-monitor.yaml
