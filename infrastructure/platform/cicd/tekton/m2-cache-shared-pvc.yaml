apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: m2-cache-shared
  namespace: payu-cicd
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-csi
  resources:
    requests:
      storage: 20Gi
