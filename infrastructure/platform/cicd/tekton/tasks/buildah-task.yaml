apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: buildah
  namespace: payu-cicd
  labels:
    app.kubernetes.io/part-of: payu
spec:
  description: "Build and push container images using Buildah (rootless)"
  params:
    - name: IMAGE
      type: string
      description: "Full image reference (registry/namespace/name:tag)"
    - name: CONTAINERFILE
      type: string
      description: Path to Containerfile
      default: "./Containerfile"
    - name: CONTEXT
      type: string
      description: Build context directory
      default: "."
    - name: BUILD_ARGS
      type: array
      description: Additional build arguments
      default: []
    - name: TLSVERIFY
      type: string
      description: Verify TLS when pushing
      default: "true"
    - name: GIT_SHA
      type: string
      description: Git commit SHA for reproducible build labels
      default: ""
  workspaces:
    - name: source
      description: Source code workspace
    - name: dockerconfig
      description: Docker registry credentials
      optional: true
  results:
    - name: BUILT_IMAGE_DIGEST
      description: SHA digest of the built image
    - name: BUILT_IMAGE_URL
      description: Full URL of the pushed image
  steps:
    - name: build-and-push
      image: "registry.redhat.io/rhel9/buildah@sha256:c62b2318eb4709c216ad25969abae5ff6b56e9879d266b539a46fdfc99e8361e"
      workingDir: $(workspaces.source.path)
      securityContext:
        capabilities:
          add: ["SETFCAP"]
      script: |
        #!/bin/bash
        set -euo pipefail

        image_registry="$(params.IMAGE)"
        image_registry="${image_registry%%/*}"

        export REGISTRY_AUTH_FILE=/tmp/registry-auth.json
        if [[ -f "$(workspaces.dockerconfig.path)/.dockerconfigjson" ]]; then
          cp "$(workspaces.dockerconfig.path)/.dockerconfigjson" "$REGISTRY_AUTH_FILE"
        else
          printf '{"auths":{}}' > "$REGISTRY_AUTH_FILE"
        fi

        # Merge the short-lived PipelineRun service-account token without
        # discarding the Red Hat registry credential used for the base image.
        if [[ "$image_registry" == *"openshift-image-registry"* ]] && [[ -f /var/run/secrets/kubernetes.io/serviceaccount/token ]]; then
          token=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)
          printf '%s' "$token" | buildah login \
            --authfile "$REGISTRY_AUTH_FILE" \
            --tls-verify=$(params.TLSVERIFY) \
            --username unused \
            --password-stdin \
            "$image_registry"
        fi

        # Extract reproducible build metadata from source
        BUILD_DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        GIT_SHA="$(params.GIT_SHA)"
        if [ -z "$GIT_SHA" ] && [ -d "$(workspaces.source.path)/.git" ]; then
          GIT_SHA="$(cd $(workspaces.source.path) && git rev-parse HEAD 2>/dev/null || echo unknown)"
        fi
        if [ -z "$GIT_SHA" ]; then
          GIT_SHA="unknown"
        fi
        BUILDER_ID="tekton-pipeline/$(cat /var/run/secrets/kubernetes.io/serviceaccount/namespace 2>/dev/null || echo payu-cicd)"

        echo "[*] Building image: $(params.IMAGE)"
        echo "    BUILD_DATE : $BUILD_DATE"
        echo "    GIT_SHA    : $GIT_SHA"
        echo "    BUILDER_ID : $BUILDER_ID"

        buildah build \
          --storage-driver=vfs \
          --format=oci \
          --tls-verify=$(params.TLSVERIFY) \
          --label "org.opencontainers.image.created=${BUILD_DATE}" \
          --label "org.opencontainers.image.revision=${GIT_SHA}" \
          --label "org.opencontainers.image.source=${GIT_SHA}" \
          --label "build-date=${BUILD_DATE}" \
          --label "git-sha=${GIT_SHA}" \
          --label "builder-id=${BUILDER_ID}" \
          -f $(params.CONTAINERFILE) \
          -t $(params.IMAGE) \
          $(params.CONTEXT)

        echo "[*] Pushing image: $(params.IMAGE)"
        buildah push \
          --storage-driver=vfs \
          --tls-verify=$(params.TLSVERIFY) \
          --digestfile /tmp/image-digest \
          $(params.IMAGE)

        DIGEST=$(cat /tmp/image-digest)
        echo -n "$DIGEST" | tee $(results.BUILT_IMAGE_DIGEST.path)
        echo -n "$(params.IMAGE)" | tee $(results.BUILT_IMAGE_URL.path)
        mkdir -p "$(workspaces.source.path)/.tekton-results"
        echo -n "$(params.IMAGE)@$DIGEST" > "$(workspaces.source.path)/.tekton-results/image-url"

        echo ""
        echo "[✓] Image pushed: $(params.IMAGE)@$DIGEST"
      volumeMounts:
        - name: varlibcontainers
          mountPath: /var/lib/containers
  volumes:
    - name: varlibcontainers
      emptyDir: {}
