apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: payu-environments
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/component: gitops
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - list:
        elements:
          - name: dev
            path: infrastructure/workloads/overlays/dev
            namespace: payu-dev
            project: payu-dev
          - name: sit
            path: infrastructure/workloads/overlays/sit
            namespace: payu-sit
            project: payu-sit
          - name: uat
            path: infrastructure/workloads/overlays/uat
            namespace: payu-uat
            project: payu-uat
          - name: preprod
            path: infrastructure/workloads/overlays/preprod
            namespace: payu-preprod
            project: payu-preprod
          - name: prod
            path: infrastructure/workloads/overlays/prod
            namespace: payu
            project: payu

  template:
    metadata:
      name: "payu-{{.name}}"
      labels:
        app.kubernetes.io/part-of: payu
        app.kubernetes.io/component: environment
        payu.io/environment: "{{.name}}"
    spec:
      project: "{{.project}}"
      source:
        repoURL: https://github.com/fajjarnr/payu.git
        targetRevision: main
        path: "{{.path}}"
      destination:
        namespace: "{{.namespace}}"
        server: https://kubernetes.default.svc
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
          allowEmpty: false
        syncOptions:
          - CreateNamespace=true
          - PrunePropagationPolicy=foreground
          - PruneLast=true
          - RespectIgnoreDifferences=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
        managedNamespaceMetadata:
          labels:
            app.kubernetes.io/part-of: payu
            payu.io/environment: "{{.name}}"
            payu.io/environment-class: stable
          annotations:
            argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
      ignoreDifferences:
        - group: apps
          kind: Deployment
          jqPathExpressions:
            - .spec.template.metadata.annotations
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: payu-monitoring
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/component: observability
spec:
  generators:
    - list:
        elements:
          - name: observability
            path: infrastructure/platform/observability
            namespace: openshift-monitoring

  template:
    metadata:
      name: "monitoring-{{name}}"
      labels:
        app.kubernetes.io/part-of: payu
        app.kubernetes.io/component: observability
    spec:
      project: payu-monitoring
      source:
        repoURL: https://github.com/fajjarnr/payu.git
        targetRevision: main
        path: "{{path}}"
      destination:
        namespace: "{{namespace}}"
        server: https://kubernetes.default.svc
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: payu-devsecops-platform
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/component: security-platform
spec:
  generators:
    - list:
        elements:
          - name: kyverno
            path: infrastructure/platform/security/kyverno
            namespace: kyverno
          - name: falco
            path: infrastructure/platform/security/falco
            namespace: falco-system
          - name: wazuh
            path: infrastructure/platform/security/wazuh
            namespace: wazuh
          - name: vault
            path: infrastructure/platform/security/vault
            namespace: payu-dev
          - name: acs
            path: infrastructure/platform/devsecops-tooling/acs
            namespace: stackrox
          - name: cosign
            path: infrastructure/platform/devsecops-tooling/cosign
            namespace: payu-cicd
          - name: compliance-operator
            path: infrastructure/platform/devsecops-tooling/compliance-operator
            namespace: openshift-compliance

  template:
    metadata:
      name: "platform-{{name}}"
      labels:
        app.kubernetes.io/part-of: payu
        app.kubernetes.io/component: security-platform
    spec:
      project: payu-platform
      source:
        repoURL: https://github.com/fajjarnr/payu.git
        targetRevision: main
        path: "{{path}}"
      destination:
        namespace: "{{namespace}}"
        server: https://kubernetes.default.svc
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - SkipDryRunOnMissingResource=true
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: payu-pr-previews
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/component: pr-preview
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - pullRequest:
        github:
          owner: fajjarnr
          repo: payu
          tokenRef:
            secretName: github-token
            key: token
        requeueAfterSeconds: 180

  template:
    metadata:
      name: "payu-preview-pr-{{.number}}"
      labels:
        app.kubernetes.io/part-of: payu
        app.kubernetes.io/component: pr-preview
        payu.io/pr-number: "{{.number}}"
        payu.io/commit-sha: "{{.head_short_sha}}"
      annotations:
        argocd.argoproj.io/preview-environment: "true"
        pr-url: "{{.url}}"
        branch-name: "{{.branch}}"
    spec:
      project: payu-preview
      source:
        repoURL: https://github.com/fajjarnr/payu.git
        targetRevision: "{{.head_sha}}"
        path: infrastructure/workloads/overlays/dev
        kustomize:
          namespace: "payu-dev-pr-{{.number}}"
          commonLabels:
            app.kubernetes.io/part-of: payu
            payu.io/environment: preview
            payu.io/pr-number: "{{.number}}"
      destination:
        namespace: "payu-dev-pr-{{.number}}"
        server: https://kubernetes.default.svc
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        managedNamespaceMetadata:
          labels:
            app.kubernetes.io/part-of: payu
            payu.io/environment: preview
            payu.io/environment-class: ephemeral
            payu.io/pr-number: "{{.number}}"
            auto-cleanup: "true"
      ignoreDifferences:
        - group: argoproj.io
          kind: Application
          jsonPointers:
            - /status