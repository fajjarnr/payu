apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# PayU API Management
# Apply with: oc apply -k infrastructure/platform/api-management/

resources:
  - 3scale/operator-install.yaml
  - 3scale/3scale-network-policy.yaml
  - 3scale/apicast-policy.yaml

# Apply only after production external DB/Redis secrets exist:
# - 3scale/apimanager.yaml
# - 3scale/payu-capabilities.yaml
