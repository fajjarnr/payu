apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# PayU Cost Optimization — Cluster-wide
# Cluster-level cost management tools and policies
# Apply with: oc apply -k infrastructure/platform/cost/

resources:
  - cluster-autoscaler.yaml
  - cost-allocation.yaml
  - budget-alerts.yaml
  - idle-resource-detector.yaml
  - opencost/
