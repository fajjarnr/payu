# ============================================================
# DISABLED: PostgreSQL HA via Crunchy operator (SUPERSEDED by postgres-statefulset.yaml (native streaming replication, iter 59))
# ============================================================
#
# DO NOT APPLY THIS FILE. The Crunchy PostgresCluster spec requires
# image versions that are NOT available in the cluster's image registry:
#   - crunchy-pgbackrest:ubi8-2.50.1 (manifest unknown)
#   - crunchy-pgbouncer:ubi8-1.22.1 (manifest unknown)
#   - registry.connect.redhat.com/crunchydata/crunchy-postgres (auth required)
#
# Current state: payu-dev uses payu-postgres-0 StatefulSet (single instance).
# See: postgres-statefulset.yaml (ACTIVE)
#
# To enable HA migration in future:
# 1. Verify Crunchy image versions in registry: podman search crunchydata
# 2. Update image tags in spec below
# 3. Add to kustomization.yaml resources list
# 4. Test: oc apply -k infrastructure/platform/data/base/
# 5. Migrate data: pg_dump from payu-postgres-0 → new cluster
# ============================================================
