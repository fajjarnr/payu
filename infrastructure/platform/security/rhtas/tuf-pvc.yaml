apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rhtas-tuf
  namespace: trusted-artifact-signer
  labels:
    app.kubernetes.io/name: rhtas-tuf
    app.kubernetes.io/part-of: payu
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-csi
  resources:
    requests:
      storage: 1Gi

