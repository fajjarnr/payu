apiVersion: v1
kind: Service
metadata:
  name: coraza-waf
  namespace: coraza-waf
  labels:
    app.kubernetes.io/name: coraza-waf
    app.kubernetes.io/part-of: payu
  annotations:
    payu.io/security-zone: perimeter
spec:
  selector:
    app.kubernetes.io/name: coraza-waf
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: coraza-waf-payudev
  namespace: payu-dev
  labels:
    app.kubernetes.io/name: coraza-waf
  annotations:
    payu.io/waf: "coraza-spoa"
spec:
  type: ExternalName
  externalName: coraza-waf.coraza-waf.svc.cluster.local
  ports:
    - port: 8080
      protocol: TCP
