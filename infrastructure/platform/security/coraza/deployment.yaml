apiVersion: apps/v1
kind: Deployment
metadata:
  name: coraza-waf
  namespace: coraza-waf
  labels:
    app.kubernetes.io/name: coraza-waf
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/component: waf
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: coraza-waf
  template:
    metadata:
      labels:
        app.kubernetes.io/name: coraza-waf
        app.kubernetes.io/part-of: payu
        app.kubernetes.io/component: waf
      annotations:
        payu.io/security-zone: perimeter
        compliance.payu.io/pci-dss: "6.4.2"
    spec:
      serviceAccountName: default
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: coraza-waf
          image: python:3.12-alpine
          imagePullPolicy: IfNotPresent
          command: ["python3", "/etc/coraza/waf-proxy.py"]
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          env:
            - name: PORT
              value: "8080"
            - name: UPSTREAM_HOST
              value: gateway-service.payu-dev.svc.cluster.local
            - name: UPSTREAM_PORT
              value: "8080"
            - name: WAZUH_HOST
              value: wazuh-manager.wazuh.svc.cluster.local
            - name: WAZUH_PORT
              value: "514"
            - name: PYTHONUNBUFFERED
              value: "1"
          volumeMounts:
            - name: coraza-config
              mountPath: /etc/coraza
              readOnly: true
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1001
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
      volumes:
        - name: coraza-config
          configMap:
            name: coraza-waf-config
            defaultMode: 0444
        - name: tmp
          emptyDir: {}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: coraza-waf-pdb
  namespace: coraza-waf
  labels:
    app.kubernetes.io/name: coraza-waf
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: coraza-waf
