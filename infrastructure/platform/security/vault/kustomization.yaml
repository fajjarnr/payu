apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Vault Dev — lab bootstrap (vault.yaml + ESO bridge + init job)
# Production variant lives in overlays/prod/ directory.
# ============================================================

resources:
  - vault.yaml
  - vault-externalsecrets.yaml
  - vault-init-job.yaml
  - vault-snapshot-cronjob.yaml
  - vault-auto-unseal.yaml
