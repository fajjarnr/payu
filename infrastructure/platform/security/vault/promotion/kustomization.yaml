apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - aws-credentials.yaml
  - vault-ha.yaml
  - bootstrap-config.yaml
  - eso-stores.yaml
  - snapshot.yaml
