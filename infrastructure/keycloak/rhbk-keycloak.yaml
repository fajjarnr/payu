###############################################################################
# Red Hat build of Keycloak (RHBK) - Keycloak CR
# Migrated from RHSSO (keycloak.org/v1alpha1) → RHBK (k8s.keycloak.org/v2alpha1)
# API Reference: https://www.keycloak.org/operator/basic-deployment
###############################################################################
apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: payu-keycloak
  namespace: payu-dev
  labels:
    app: payu-keycloak
    app.kubernetes.io/component: sso
    app.kubernetes.io/name: payu-keycloak
    app.kubernetes.io/part-of: payu
    environment: dev
spec:
  instances: 1

  # --- Bootstrap Admin (initial admin user) ---
  bootstrapAdmin:
    user:
      secret: payu-keycloak-admin

  # --- Database Configuration (Crunchy PGO) ---
  db:
    vendor: postgres
    host: payu-postgres-primary.payu-dev.svc
    port: 5432
    database: keycloak
    usernameSecret:
      name: payu-keycloak-db
      key: username
    passwordSecret:
      name: payu-keycloak-db
      key: password

  # --- HTTP Configuration ---
  http:
    httpEnabled: true
    httpPort: 8080
    httpsPort: 8443

  # --- Hostname Configuration ---
  hostname:
    hostname: https://sso.dev.payu.fajjjar.my.id
    strict: false
    backchannelDynamic: true

  # --- Ingress (OpenShift Route) ---
  ingress:
    enabled: true

  # --- Proxy Headers ---
  proxy:
    headers: xforwarded

  # --- Features ---
  features:
    enabled:
      - token-exchange
      - admin-fine-grained-authz

  # --- Additional Keycloak server options ---
  additionalOptions:
    # Health endpoint
    - name: health-enabled
      value: "true"
    # Metrics endpoint (for Prometheus)
    - name: metrics-enabled
      value: "true"
