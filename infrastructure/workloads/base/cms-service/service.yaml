apiVersion: v1
kind: Service
metadata:
  name: cms-service
  labels:
    app.kubernetes.io/name: cms-service
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/version: 1.5.0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cms-service
  ports:
  - port: 8080
    targetPort: 8080
    name: http
