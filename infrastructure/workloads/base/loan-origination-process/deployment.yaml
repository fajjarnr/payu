apiVersion: apps/v1
kind: Deployment
metadata:
  name: loan-origination-process
  labels:
    app.kubernetes.io/name: loan-origination-process
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/component: process-orchestration
    app.kubernetes.io/version: 1.0.0
    team: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: loan-origination-process
  template:
    metadata:
      labels:
        app.kubernetes.io/name: loan-origination-process
        app.kubernetes.io/part-of: payu
        app.kubernetes.io/component: process-orchestration
        app.kubernetes.io/version: 1.0.0
        team: backend
    spec:
      securityContext:
        runAsNonRoot: true
      serviceAccountName: loan-origination-process
      terminationGracePeriodSeconds: 60
      volumes:
      - name: tmp
        emptyDir: {}
      containers:
      - name: app
        image: image-registry.openshift-image-registry.svc:5000/payu-dev/loan-origination-process:1.0.0
        ports:
        - containerPort: 8080
          name: http
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        readinessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 15
        startupProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 30
        resources:
          requests:
            cpu: 100m
            memory: 384Mi
          limits:
            cpu: 500m
            memory: 768Mi
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: container
        - name: SERVER_PORT
          value: '8080'
        - name: DATABASE_URL
          value: jdbc:postgresql://payu-postgres-ha-primary.payu-dev.svc.cluster.local:5432/payu_loan_origination
        - name: DATABASE_USER
          valueFrom:
            secretKeyRef:
              key: DB_USERNAME
              name: db-secrets
              optional: true
        - name: DATABASE_PASSWORD
          valueFrom:
            secretKeyRef:
              key: DB_PASSWORD
              name: db-secrets
              optional: true
        - name: KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              key: KAFKA_URL
              name: service-endpoints
        - name: LENDING_RULES_URL
          value: http://lending-rules.payu-dev.svc:8080
        - name: JAVA_TOOL_OPTIONS
          value: "--enable-native-access=ALL-UNNAMED"
        - name: PAYU_SECURITY_ENCRYPTION_SALT
          valueFrom:
            secretKeyRef:
              name: dev-env-secrets
              key: ENCRYPTION_SALT
              optional: true
        - name: WEBHOOK_SECURITY_SECRET
          valueFrom:
            secretKeyRef:
              name: dev-env-secrets
              key: WEBHOOK_SECRET
              optional: true
