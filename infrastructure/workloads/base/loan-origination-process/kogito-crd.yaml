apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kognitoruntimes.rhpam.kiegroup.org
spec:
  group: rhpam.kiegroup.org
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          # ponytail: minimal stub schema, expand if operator validation needed
          x-kubernetes-preserve-unknown-fields: true
  scope: Namespaced
  names:
    plural: kognitoruntimes
    singular: kognitoruntime
    kind: KogitoRuntime
    listKind: KogitoRuntimeList
