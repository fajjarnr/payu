apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  labels:
    app.kubernetes.io/name: transaction-service
    app.kubernetes.io/part-of: payu
    app.kubernetes.io/version: 1.8.80
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: transaction-service
  ports:
  - port: 8080
    targetPort: 8080
    name: http
