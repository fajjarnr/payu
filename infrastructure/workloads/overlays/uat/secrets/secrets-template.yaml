# UAT Secrets Template — Replace placeholders with actual values via Vault/ESO
apiVersion: v1
kind: Secret
metadata:
  name: uat-secrets
  namespace: payu-uat
  labels:
    app.kubernetes.io/part-of: payu
  annotations:
    external-secrets.io/note: "Managed by External Secrets Operator from Vault"
type: Opaque
stringData:
  DB_PASSWORD: "${VAULT:secret/data/payu-uat/db#password}"
  DB_USERNAME: "${VAULT:secret/data/payu-uat/db#username}"
  REDIS_PASSWORD: "${VAULT:secret/data/payu-uat/redis#password}"
  KEYCLOAK_CLIENT_SECRET: "${VAULT:secret/data/payu-uat/keycloak#client-secret}"
  KAFKA_SASL_PASSWORD: "${VAULT:secret/data/payu-uat/kafka#password}"
