<?xml version="1.0" encoding="UTF-8"?>
<!--
    Infinispan 15.2 server config, matching Data Grid 8.6 protocol behavior.
    RESP shares the endpoint port 11222, as it does on OpenShift.
    Authentication is enabled on RESP using the default properties realm.
    The container's USER/PASS env vars populate this realm via launch.sh.
-->
<infinispan
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="urn:infinispan:config:15.2 https://infinispan.org/schemas/infinispan-config-15.2.xsd
                            urn:infinispan:server:15.2 https://infinispan.org/schemas/infinispan-server-15.2.xsd"
        xmlns="urn:infinispan:config:15.2"
        xmlns:server="urn:infinispan:server:15.2">

    <cache-container name="default" statistics="true">
        <jmx enabled="false"/>
        <security>
            <authorization/>
        </security>
    </cache-container>

    <server xmlns="urn:infinispan:server:15.2">
        <interfaces>
            <interface name="public">
                <inet-address value="${infinispan.bind.address:127.0.0.1}"/>
            </interface>
        </interfaces>

        <socket-bindings default-interface="public" port-offset="${infinispan.socket.binding.port-offset:0}">
            <socket-binding name="default" port="${infinispan.bind.port:11222}"/>
        </socket-bindings>

        <security>
            <security-realms>
                <security-realm name="default">
                    <properties-realm/>
                </security-realm>
            </security-realms>
        </security>

        <endpoints socket-binding="default" security-realm="default">
            <endpoint>
                <rest-connector/>
                <hotrod-connector/>
                <resp-connector security-realm="default"/>
            </endpoint>
        </endpoints>
    </server>
</infinispan>
