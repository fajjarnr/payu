# Generated quadlet file for wallet-service
# This file can be used with systemd to manage the container

[Container]
Image=localhost/wallet-service:dev
ContainerName=wallet-service
AutoUpdate=registry
Network=payu-network
PodmanArgs=--label=io.payu.service=wallet-service
# Add your custom arguments below
# EnvironmentFile=/wallet-service/env
# Volume=/wallet-service/config:/etc/wallet-service:ro
# Volume=/wallet-service/data:/var/lib/wallet-service

[Service]
# Restart on failure, after 5 seconds
Restart=on-failure
RestartSec=5s

# Health check
ExecStartPre=/usr/bin/podman check-network payu-network
ExecStartPre=/usr/bin/podman pull localhost/wallet-service:dev

# Set resource limits
# LimitMEM=512M
# LimitCPU=1.0

# Security settings
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true

[Install]
WantedBy=default.target
