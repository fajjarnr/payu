apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ab-testing-service.yaml
  - account-service.yaml
  - analytics-service.yaml
  - api-portal-service.yaml
  - auth-service.yaml
  - backoffice-service.yaml
  - bi-fast-simulator.yaml
  - billing-service.yaml
  - cms-service.yaml
  - compliance-service.yaml
  - dukcapil-simulator.yaml
  - fx-service.yaml
  - gateway-service.yaml
  - investment-service.yaml
  - kyc-service.yaml
  - lending-service.yaml
  - notification-service.yaml
  - partner-service.yaml
  - promotion-service.yaml
  - qris-simulator.yaml
  - statement-service.yaml
  - support-service.yaml
  - transaction-service.yaml
  - wallet-service.yaml
  - web-app.yaml

# Add ConfigMapGenerator for shared config if needed, but current manifests use inline configs or placeholders.
# Secrets should be managed securely (e.g. sealed secrets or external secrets), 
# but for this base, we assume they exist or are created separately.
